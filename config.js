const config = {
    db: {
        database: 'userLoginDB',
        server: 'localhost',
        user: 'michal',
        password: '12345678',
        options: {
            trustServerCertificate: true
        }
    }
}

module.exports = config;